# Python Image Slider (Tkinter)

This project is a simple **image slider** built using **Python** and **Tkinter**.  
The app displays multiple images like an album, and by clicking on the image (left-click), the next image will be shown — similar to a gallery or slideshow.

---

## 🚀 Features
- Displays multiple images in a sequence  
- Move to the **next image** by simply **left-clicking** on the current image  
- Smooth and simple slide behavior  
- Easy-to-understand Tkinter implementation  
- Suitable as a beginner-friendly GUI project

---

## 🖼️ How It Works
1. Several images are loaded inside the program  
2. The user clicks on the displayed image  
3. The slider switches to the next image  
4. When the last image is reached, it can loop back to the first (if implemented)

---

## 🛠️ Technologies Used
- **Python 3.x**
- **Tkinter** for GUI
- **Pillow (PIL)** for handling images (if used in your project)

---

## 📂 Project Structure
